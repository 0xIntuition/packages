# Summary

<!-- What this PR does and why. For release-train child PRs, include the P-number (e.g. P03). -->

## Package impact

<!-- Which packages change, and how (new package, behavior change, dependency change, docs only, none). -->

| Package | Change |
| --- | --- |
|  |  |

## Target versions

<!-- Target version for every changed package, and any internal exact-pin updates this forces. "n/a" if no manifest changes. -->

## Identity impact

<!-- Required. One of:
  - `Identity impact: none — existing golden bytes unchanged`
  - `Identity impact: intentional — fixture IDs <X/Y> change; migration decision <D> approved`
Identity-sensitive surfaces: scheme grammar/validation/canonicalization, NORM-1/gen1 algorithms,
classification ladder order or source fields, profile selection/serialization, atom data bytes or
ID calculation, predicate atom bytes. -->

Identity impact:

## Fixture impact

<!-- Conformance/golden fixtures added, changed, or removed, with fixture IDs. "none" if untouched. -->

## Contract artifact

<!-- If ABI/bytecode or generated contract types change: source package, exact version, integrity,
and ABI fingerprint. "n/a" otherwise. -->

## Rollout notes

<!-- Consumer-facing effects: migration steps, compatibility guarantees, publish-order or
release-age constraints, follow-up PRs this enables or blocks. -->
